# 🚀 Установка и настройка Strapi v5 на VPS (Ubuntu 22.04)

Этот гайд поможет вам развернуть Strapi v5 на вашем VPS сервере рядом с основным сайтом.

---

## 1. Подготовка (на VPS)

Убедитесь, что вы находитесь в директории, где хотите хранить бэкенд (но **НЕ** внутри папки `businessform`, лучше рядом).

```bash
cd ~
```

## 2. Установка Strapi

Запустите команду создания проекта.
**Важно:** Strapi требует Node.js >= 18.0.0 (у вас v20, это отлично).

```bash
npx create-strapi-app@latest backend --quickstart
```
*(Соглашайтесь на установку пакетов. Это займет 2-5 минут)*

После установки Strapi автоматически запустится. Остановите его (`Ctrl + C`), чтобы настроить.

---

## 3. Настройка CORS и Хоста (КРИТИЧНО для v5!)

Нам нужно разрешить вашему фронтенду (который работает на этом же сервере или локально) обращаться к Strapi.

Откройте файл конфигурации:

```bash
nano backend/config/middlewares.ts
```

Найдите блок `strapi::cors` и замените его на (или отредактируйте):

```typescript
// ... внутри массива export default [
  {
    name: 'strapi::cors',
    config: {
      origin: [
        'http://localhost:5000',      // Локальный тест
        'http://0.0.0.0:5000',        // Серверный тест
        'http://businessform.xorek.cloud', // Ваш домен (ОБЯЗАТЕЛЬНО)
        'https://businessform.xorek.cloud',
        'http://193.233.85.240',      // Ваш IP
        'http://localhost:5173',      // Локальная разработка Vite
      ],
      methods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'HEAD', 'OPTIONS'],
      headers: ['Content-Type', 'Authorization', 'Origin', 'Accept'],
      keepHeaderOnError: true,
    },
  },
// ...
```

Так же настроим сервер слушать **все интерфейсы**, а не только localhost.
Откройте `backend/config/server.ts`:

```bash
nano backend/config/server.ts
```

Измените `host`:

```typescript
export default ({ env }) => ({
  host: env('HOST', '0.0.0.0'), // Было 'localhost', меняем на '0.0.0.0'
  port: env.int('PORT', 1337),
  // ... остальное без изменений
});
```

---

## 4. Запуск в фоновом режиме (PM2)

У вас уже есть PM2. Давайте добавим Strapi туда.

1.  Создайте файл `server.js` (entry point для PM2) в корне папки `backend`:
    ```bash
    cd ~/backend
    nano server.js
    ```
    Вставьте:
    ```javascript
    const strapi = require('@strapi/strapi');
    strapi().start();
    ```

2.  Соберите админку:
    ```bash
    npm run build
    ```

3.  Запустите через PM2:
    ```bash
    pm2 start server.js --name strapi
    pm2 save
    ```

Теперь Strapi работает на порту `1337`.
Доступен по адресу: `http://193.233.85.240:1337/admin`

---

## 5. Создание Структуры Данных (Content Types)

Зайдите в админку (`/admin`), создайте первого администратора.

### 5.1 News Item (Новости)
Создайте Collection Type: **NewsItem** (Display name: `News Item`)

Поля:
*   `title` (Text -> Short Text) — **Required**
*   `summary` (Text -> Long Text)
*   `content` (Rich Text -> Blocks или Markdown) — для v5 лучше Blocks.
*   `publishedAt` (Date) — выберите Date, не Datetime, если не нужно время.
*   `category` (Text -> Short Text) — пока просто текстом (или Enum: legislation, taxes, social).
*   `image` (Media -> Single Media).

### 5.2 FAQ (Вопросы)
Создайте Collection Type: **Faq** (Display name: `Faq`)

Поля:
*   `question` (Text -> Short Text) — **Required**
*   `answer` (Rich Text -> Blocks) — **Required**
*   `category` (Enumeration) — значения: `main`, `taxes`, `registration`.

### 5.3 Разрешения (Permissions) - ОЧЕНЬ ВАЖНО 🚨
Чтобы фронтенд видел данные без авторизации:
1.  Перейдите в **Settings** -> **Users & Permissions Plugin** -> **Roles**.
2.  Выберите **Public**.
3.  Найдите **News-item** -> поставьте галочку **find** и **findOne**.
4.  Найдите **Faq** -> поставьте галочку **find** и **findOne**.
5.  Нажмите **Save**.

Без этого API будет возвращать `403 Forbidden`.

---

## 6. Настройка Nginx (Опционально, но желательно)

Чтобы Strapi был доступен на поддомене `api.businessform.xorek.cloud` (красиво и безопасно), нужно создать конфиг Nginx.
Но пока можно работать напрямую по IP: `http://193.233.85.240:1337`.

**Обязательно обновите `.env` на фронтенде:**
```bash
cd ~/businessform
nano .env
```
Добавьте/Измените:
```ini
VITE_STRAPI_URL=http://193.233.85.240:1337
```
И перезапустите фронтенд: `pm2 restart bizstart`

---

**Готово!** Теперь наполните Strapi парой тестовых новостей, и мы можем приступать к интеграции на фронтенде.
