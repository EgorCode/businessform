# 🚀 CRM и Telegram Интеграция для BizStartMaster

## 📋 Обзор

В проект добавлены:
- **CRM система** с управлением пользователями и ролями
- **Telegram бот** для автоматической публикации новостей
- **Безопасная аутентификация** с JWT токенами
- **Панель администратора** с полным функционалом

## 🔐 Установка и настройка

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте и отредактируйте файл `.env`:

```env
# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Admin Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_in_production

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
BASE_URL=http://localhost:5000

# Google Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
```

### 3. Инициализация базы данных
```bash
npm run db:init
```

### 4. Запуск проекта
```bash
npm run dev
```

## 🤖 Настройка Telegram бота

### 1. Создание бота
1. Найдите `@BotFather` в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота: `BizStartMaster Bot`
4. Получите токен и сохраните его в `.env`

### 2. Настройка канала
1. Создайте канал для новостей
2. Сделайте бота администратором канала
3. Получите ID канала (обычно начинается с `-100`)

### 3. Подключение канала
1. Зайдите в административную панель
2. Перейдите в раздел "Telegram"
3. Нажмите "Добавить канал"
4. Введите данные канала

## 🔐 Доступ к административной панели

### URL доступа
- **Вход**: `http://localhost:5000/admin/login`
- **Панель**: `http://localhost:5000/admin/dashboard`

### Учетные данные по умолчанию
- **Логин**: `admin`
- **Пароль**: `change_this_in_production`

⚠️ **Обязательно измените пароль после первого входа!**

## 👥 Управление пользователями

### Роли пользователей
- **Admin**: Полный доступ ко всем функциям
- **Editor**: Доступ к контенту и Telegram интеграции
- **User**: Базовый доступ (в разработке)

### Создание пользователей
1. Войдите как администратор
2. Перейдите в вкладку "Пользователи"
3. Заполните форму создания пользователя
4. Выберите appropriate роль

## 📱 Telegram интеграция

### Функциональность
- **Автоматическое получение постов** из Telegram каналов
- **Модерация контента** перед публикацией
- **Автоматическая публикация** одобренных постов
- **Управление каналами** (включение/выключение)

### Процесс работы
1. **Публикация в Telegram** → Пост появляется в канале
2. **Автоматический импорт** → Пост сохраняется в систему
3. **Модерация** → Администратор проверяет контент
4. **Публикация на сайте** → Одобренные посты появляются на сайте

### Настройка вебхука
```bash
# Для разработки
curl -X POST https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook \
  -d "url=http://localhost:5000/api/telegram/webhook"

# Для продакшена
curl -X POST https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook \
  -d "url=https://yourdomain.com/api/telegram/webhook"
```

## 🔒 Безопасность

### JWT токены
- **Срок действия**: 24 часа
- **Автоматическое обновление**: при каждом запросе
- **Безопасное хранение**: в localStorage

### Защита API
- **Rate limiting**: 100 запросов в час на AI эндпоинты
- **Валидация данных**: Zod схемы для всех входных данных
- **Ролевая модель**: Разграничение доступа по ролям

## 📊 Структура базы данных

### Новые таблицы
```sql
-- Пользователи
users (id, username, password, role, created_at)

-- Сессии
user_sessions (id, user_id, token, expires_at)

-- Telegram каналы
telegram_channels (id, name, telegram_id, title, is_active, created_at)

-- Telegram посты
telegram_posts (id, channel_id, post_id, content, media_urls, published_at, status, processed_at)
```

## 🛠️ API эндпоинты

### Аутентификация
- `POST /api/admin/login` - Вход
- `POST /api/admin/logout` - Выход
- `GET /api/admin/me` - Текущий пользователь

### Управление пользователями
- `GET /api/admin/users` - Список пользователей
- `POST /api/admin/users` - Создание пользователя
- `PUT /api/admin/users/:id` - Обновление пользователя
- `DELETE /api/admin/users/:id` - Удаление пользователя

### Telegram интеграция
- `GET /api/telegram/channels` - Список каналов
- `POST /api/telegram/channels` - Добавление канала
- `PUT /api/telegram/channels/:id/toggle` - Переключение канала
- `GET /api/telegram/posts/pending` - Ожидающие посты
- `POST /api/telegram/posts/:id/approve` - Одобрение поста
- `POST /api/telegram/posts/:id/reject` - Отклонение поста
- `POST /api/telegram/start` - Запуск бота

## 🎯 Использование

### Базовый рабочий процесс
1. **Настройка бота** через @BotFather
2. **Создание канала** в Telegram
3. **Подключение канала** в административной панели
4. **Настройка модераторов** с ролями Editor
5. **Тестирование** публикацией тестового поста
6. **Мониторинг** через панель модерации

### Продвинутые возможности
- **Множественные каналы**: Подключение нескольких Telegram каналов
- **Автоматическая категоризация**: AI анализ контента
- **Медиа поддержка**: Автоматическая загрузка изображений
- **График публикаций**: Отложенная публикация

## 🔧 Разработка

### Структура файлов
```
server/
├── middleware/
│   └── auth.ts              # JWT аутентификация
├── routes/
│   ├── admin.ts              # API администратора
│   └── telegram.ts           # API Telegram интеграции
└── services/
    ├── authService.ts         # Сервис аутентификации
    └── telegramBotService.ts  # Сервис Telegram бота

client/src/
├── pages/
│   ├── AdminLogin.tsx         # Страница входа
│   └── AdminDashboard.tsx     # Панель администратора
└── components/admin/
    └── TelegramIntegration.tsx  # Компонент Telegram
```

### Кастомизация
- **Роли**: Легко добавить новые роли в `auth.ts`
- **Права доступа**: Настроить в middleware
- **UI компоненты**: Использовать существующие UI компоненты
- **Стили**: Tailwind CSS для консистентности

## 🚨 Важные замечания

### Безопасность
1. **Измените пароли** по умолчанию
2. **Используйте HTTPS** в продакшене
3. **Ограничьте доступ** к административной панели
4. **Регулярно обновляйте** зависимости

### Производительность
1. **Мониторьте** нагрузку на API
2. **Оптимизируйте** запросы к базе данных
3. **Используйте** кэширование для AI запросов
4. **Настраивайте** rate limiting

### Резервное копирование
1. **Резервные копии** базы данных
2. **Экспорт конфигурации** ботов
3. **Логирование** действий администраторов
4. **Мониторинг** работы системы

## 📞 Поддержка

### Частые проблемы
- **Бот не отвечает**: Проверьте токен и вебхук
- **Нет доступа**: Проверьте роль пользователя
- **Не публикуются посты**: Проверьте статус канала
- **Ошибка JWT**: Обновите токен в localStorage

### Логирование
```javascript
// Включите логирование в консоли
localStorage.setItem('debug', 'true');

// Проверьте ошибки в Network вкладке
// Используйте console.log для отладки
```

## 🎉 Заключение

Система готова к использованию! После настройки вы получите:

✅ **Безопасную CRM систему** с управлением пользователями  
✅ **Автоматическую публикацию** новостей из Telegram  
✅ **Модерацию контента** с гибкими настройками  
✅ **Ролевую модель** доступа для безопасности  
✅ **Современный интерфейс** на базе React + Tailwind  

Начните с настройки бота и создания пользователей, затем протестируйте публикацию постов из Telegram!