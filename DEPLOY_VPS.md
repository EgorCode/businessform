# 🚀 Гайд по развёртыванию на VPS

Этот гайд поможет вам быстро развернуть проект `businessform` на вашем VPS сервере.

## 📋 Требования
- Доступ к VPS (по SSH).
- Node.js (версия 18+).
- Git.

## 🛠️ Шаг 1: Подготовка сервера (если новый)

Подключитесь к серверу:
```bash
ssh user@your_server_ip
```

Установите Node.js и Git (для Ubuntu/Debian):
```bash
# Обновляем пакеты
sudo apt update

# Устанавливаем Git
sudo apt install git -y

# Устанавливаем Node.js (v20)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

## ⚡ Быстрый способ (через скрипт)

Мы подготовили автоматический скрипт настройки `setup.sh`.

1. **Клонируйте проект:**
   ```bash
   git clone https://github.com/EgorCode/businessform.git
   cd businessform
   ```

2. **Запустите скрипт:**
   ```bash
   chmod +x setup.sh  # Делаем скрипт исполняемым
   ./setup.sh         # Запускаем
   ```
   *Скрипт сам спросит пароль sudo для установки Node.js/PM2, установит зависимости, соберет проект и запустит его.*

3. **Не забудьте настроить .env!**
   Скрипт создаст базовый `.env`. Отредактируйте его: `nano .env`

---

## �️ Ручной способ (если скрипт не сработал)

### Шаг 2: Установка проекта (вручную)

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/EgorCode/businessform.git
   cd businessform
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```
   *Это установит зависимости и для клиента, и для сервера.*

3. **Настройте переменные окружения:**
   Создайте файл `.env`:
   ```bash
   nano .env
   ```
   Вставьте туда содержимое вашего локального `.env` (только убедитесь, что `NODE_ENV=production` и пароли надежные).
   Нажмите `Ctrl+X`, затем `Y`, затем `Enter`, чтобы сохранить.

## 🚀 Шаг 3: Сборка и Запуск

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Запустите сервер:**
   Для надежной работы используем `pm2` (менеджер процессов).
   
   Установка pm2:
   ```bash
   sudo npm install -g pm2
   ```

   Запуск:
   ```bash
   pm2 start npm --name "bizstart" -- run start
   ```

   *Сервер запустится на порту 5000 (или том, что указан в .env).*

3. **Сохраните автозапуск (чтобы работало после перезагрузки):**
   ```bash
   pm2 save
   pm2 startup
   ```

## 🔄 Обновление проекта

Когда вы выложите новые изменения на GitHub:

```bash
cd businessform
git pull
npm install  # если менялись зависимости
npm run build # пересобрать фронтенд
pm2 restart bizstart
```
