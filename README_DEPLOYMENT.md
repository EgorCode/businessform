# Развертывание BizStartMaster на VPS Ubuntu 22

## Готовый скрипт развертывания

Я создал полный автоматический скрипт развертывания [`deploy.sh`](./deploy.sh), который настраивает VPS сервер с нуля.

## Что делает скрипт

✅ **Полная автоматизация развертывания:**
- Обновление системы
- Установка Apache2, Node.js 18.x, Git
- Настройка firewall
- Конфигурация Apache с прокси для Node.js
- Клонирование и сборка проекта
- Настройка systemd сервиса
- Инициализация базы данных SQLite
- Настройка резервного копирования
- Создание скриптов обновления

## Как использовать

### 1. Подготовьте VPS сервер

Требования:
- Ubuntu 20.04+ / Debian 10+
- Доступ по SSH с правами sudo
- Минимум: 1 ядро CPU, 1 ГБ RAM, 10 ГБ диск

### 2. Скачайте и запустите скрипт

```bash
# Скачайте скрипт
wget https://raw.githubusercontent.com/your-repo/bizstartmaster/main/deploy.sh

# Сделайте исполняемым
chmod +x deploy.sh

# Запустите с правами sudo
sudo ./deploy.sh
```

### 3. С параметрами (опционально)

```bash
sudo ./deploy.sh "REPO_URL" "DOMAIN" "EMAIL" "GEMINI_API_KEY"
```

Пример:
```bash
sudo ./deploy.sh "https://github.com/user/bizstartmaster.git" "mysite.com" "admin@mysite.com" "AIzaSy..."
```

## После развертывания

### Проверка работы
```bash
# Статус сервиса
systemctl status bizstartmaster

# Проверка API
curl http://your-domain.com/api/ai/health

# Просмотр логов
journalctl -u bizstartmaster -f
```

### Обновление приложения
```bash
# Автоматическое обновление
sudo update-bizstartmaster.sh
```

### Резервное копирование
```bash
# Ручной бэкап
sudo backup-bizstartmaster.sh

# Автоматические бэкапы ежедневно в 2:00
# Хранятся в /var/backups/bizstartmaster/
```

## Файлы созданные скриптом

- **Apache конфигурация**: `/etc/apache2/sites-available/bizstartmaster.conf`
- **Systemd сервис**: `/etc/systemd/system/bizstartmaster.service`
- **Переменные окружения**: `/var/www/bizstartmaster/.env.production`
- **Проект**: `/var/www/bizstartmaster`
- **Скрипт обновления**: `/usr/local/bin/update-bizstartmaster.sh`
- **Скрипт бэкапа**: `/usr/local/bin/backup-bizstartmaster.sh`

## Полезные команды

```bash
# Перезапуск сервисов
sudo systemctl restart bizstartmaster
sudo systemctl restart apache2

# Проверка конфигурации Apache
sudo apache2ctl configtest

# Просмотр логов Apache
sudo tail -f /var/log/apache2/bizstartmaster_access.log
sudo tail -f /var/log/apache2/bizstartmaster_error.log
```

## Настройка SSL (рекомендуется)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-apache

# Получение сертификата
sudo certbot --apache -d your-domain.com
```

## Безопасность

Скрипт автоматически настраивает:
- Firewall с открытыми портами 80, 443, SSH
- Заголовки безопасности в Apache
- Автоматические обновления системы (рекомендуется включить)

Дополнительные рекомендации:
1. Используйте надежные пароли и SSH ключи
2. Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade -y`
3. Мониторьте логи на предмет подозрительной активности

## Устранение проблем

### Сервис не запускается
```bash
sudo systemctl status bizstartmaster
sudo journalctl -u bizstartmaster -n 50
```

### Ошибки Apache
```bash
sudo apache2ctl configtest
sudo tail -f /var/log/apache2/bizstartmaster_error.log
```

### Проблемы с БД
```bash
cd /var/www/bizstartmaster
sudo -u www-data npm run db:check
```

## Документация

- [Полный скрипт развертывания](./DEPLOYMENT_SCRIPT.md) - подробное описание скрипта
- [Инструкция по развертыванию](./DEPLOYMENT_INSTRUCTIONS.md) - краткая инструкция
- [Руководство по развертыванию](./DEPLOYMENT_GUIDE.md) - полное руководство
- [Быстрое развертывание](./QUICK_DEPLOYMENT.md) - альтернативный способ

## Готово!

После выполнения скрипта ваше приложение BizStartMaster будет полностью развернуто и доступно по указанному домену или IP адресу.

Приложение включает:
- 🚀 Платформа для помощи в запуске бизнеса
- 🤖 AI-ассистент на базе Google Gemini
- 📰 Новостной раздел
- 🧮 Налоговые калькуляторы
- 📚 База знаний

Все компоненты автоматически настроены и готовы к работе!